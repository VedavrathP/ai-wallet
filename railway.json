{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "service/Dockerfile"
  },
  "deploy": {
    "startCommand": "cd service && alembic upgrade head && python -m agent_wallet_service.scripts.seed && uvicorn agent_wallet_service.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
